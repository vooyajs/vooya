# `.voo` history

`.voo` was an early manifest experiment used while Vooya's island boundary was
being explored. It is retired and is not a supported input format. New work
uses ordinary Rust files with role attributes and `rsx!`; see the
[Rust-file authoring guide](./rust-file-authoring.md).
